use pipedream_domain::player::Player;
use pipedream_engine::state_machine::StateMachine;

use pipedream_engine::state::State;

pub fn turn_end(player: &Player, machine: &StateMachine) -> State {
    // Go to next target start
    todo!()
}
